kylerloucks.github.io
=====================

Simple static landing page of my resume/portfolio hosted on GitHub Pages.

Deploy
------
- Pushes to `main` trigger the GitHub Actions `deploy.yml` workflow, which publishes to GitHub Pages automatically.
